{
  "mcpServers": {
    "aether-wcag-scanner": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/scripts/start.js"
      ],
      "env": {
        "ALLCHEMY_API_KEY": "${user_config.api_key}"
      },
      "timeout": 600000
    }
  }
}
